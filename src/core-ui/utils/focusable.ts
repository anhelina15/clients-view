import { tv } from 'tailwind-variants';

export const focusable = tv({
  base: 'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-500 focus-visible:ring-offset-2 transition-shadow',
});
